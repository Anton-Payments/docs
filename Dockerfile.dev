# Dev Dockerfile — static file server for API docs
FROM node:22-alpine

RUN npm install -g serve

WORKDIR /app

# Content is volume-mounted at runtime
COPY . .

EXPOSE 3003

CMD ["serve", "-l", "3003", "-s", "."]
