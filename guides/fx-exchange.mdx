---
title: "FX & Currency Exchange"
description: "Quote, lock, and execute foreign exchange between merchant balances."
---

<Note>
  Placeholder. Guide covers:
  - When you need FX (funding a payout in a currency you don't hold)
  - Indicative rate vs lockable quote (`GET /v1/fx/rate` vs `POST /v1/fx/quote`)
  - Executing an exchange between balances (`POST /v1/fx/exchange`)
  - Quote expiry and reconciliation
  - Listing exchange history
</Note>
