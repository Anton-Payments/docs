---
title: "IP Allowlist"
description: "Anton's outbound IP addresses for firewall and security policy configuration."
---

<Note>
  Placeholder. IPs are being stabilized and will be published here within the week.

  When finalized, this page should include:

  - **Webhook delivery IPs** — the source IPs Anton sends webhook requests from. Add these to your ingress allowlist if your webhook endpoint is firewalled.
  - **API ingress CIDR blocks** (if Anton publishes merchant-inbound ranges).
  - **Per-environment**: separate IPs for production, sandbox, and staging where applicable.
  - **Change policy**: how much notice is given before an IP set changes.
</Note>

## Production

_To be published._

## Sandbox

_To be published._
