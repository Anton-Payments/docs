---
title: "API Reference"
sidebarTitle: "Overview"
description: "Complete reference for the Anton Payments REST API."
---

## Base URL

<CodeGroup>

```bash Production
https://api.antonpayments.com
```

```bash Sandbox
https://api.antonpayments.dev
```

</CodeGroup>

## Authentication

All requests require an API key in the `Authorization` header:

```bash
Authorization: Bearer ak_live_your_key_here
```

Use `ak_test_*` keys for sandbox and `ak_live_*` keys for production. See [Authentication](/authentication) for details.

## Request format

- Request bodies use `Content-Type: application/json`
- All monetary amounts are strings (e.g., `"500.00"`) to avoid floating-point precision issues
- Dates use ISO 8601 format (`2026-02-14T10:30:00Z`)
- IDs are prefixed strings (e.g., `pay_`, `pye_`, `ins_`, `batch_`)

## Response format

All successful responses are wrapped in a `data` envelope:

```json Single resource
{
  "data": {
    "id": "pay_cng3q8s6ek9kc5qg1h2g",
    "status": "completed",
    "source_amount": "500.00"
  }
}
```

```json List response
{
  "data": [
    { "id": "pay_abc123" },
    { "id": "pay_def456" }
  ],
  "has_more": true,
  "next_cursor": "pay_def456"
}
```

See [Pagination](/concepts/pagination) for details on cursor-based pagination.

## Idempotency

Financial mutation endpoints (create payout, confirm batch, execute FX exchange) require an `Idempotency-Key` header. See [Idempotency](/concepts/idempotency) for details.

## Rate limiting

Requests are rate-limited per merchant (1,000 requests/minute). Exceeding the limit returns `429 Too Many Requests` with a `Retry-After` header.

## Resources

| Resource | Endpoint | Description |
|----------|----------|-------------|
| [Reference Data](/api-reference/reference-data/list-currencies) | `/v1/currencies`, `/v1/countries`, `/v1/payment-methods` | Supported currencies, countries, and payment methods |
| [Payouts](/api-reference/payouts/create-payout) | `/v1/payouts` | Create and track cross-border payouts |
| [Payees](/api-reference/payees/create-payee) | `/v1/payees` | Manage payout recipients |
| [Instruments](/api-reference/instruments/list-instruments) | `/v1/instruments` | Payment methods attached to payees |
| [Balances](/api-reference/balances/list-balances) | `/v1/balances` | Merchant funding balances by currency |
| [Accounts](/api-reference/accounts/list-accounts) | `/v1/accounts` | Funding accounts and transaction history |
| [Batches](/api-reference/batches/upload-batch) | `/v1/batches` | Bulk payout uploads via CSV/XLSX |
| [Webhooks](/api-reference/webhooks/create-webhook-subscription) | `/v1/webhooks` | Real-time event notifications |
| [Documents](/api-reference/documents/upload-document) | `/v1/documents` | Upload compliance documents |
| [RFIs](/api-reference/rfis/list-rfis) | `/v1/rfis` | Respond to compliance requests |
| [Pricing](/api-reference/pricing/list-merchant-pricing) | `/v1/pricing` | View pricing plans and fee quotes |
| [FX](/api-reference/fx/create-fx-quote) | `/v1/fx` | Foreign exchange quoting and conversion |
| [Corridors](/api-reference/corridors/list-corridors) | `/v1/corridors` | Available payment corridors |
